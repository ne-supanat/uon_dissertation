classDiagram
    class Commuters {
        -string name
        -string job
        -string homeLocation
        -string workLocation
        -string preferredTransportMode
        -float commuteDistance
        -float carbonFootprint
        +Commuters(string name, string job, string homeLocation, string workLocation, string preferredTransportMode, float commuteDistance)
        +string getName()
        +string getJob()
        +string getHomeLocation()
        +string getWorkLocation()
        +string getPreferredTransportMode()
        +float getCommuteDistance()
        +float getCarbonFootprint(): float
        +void setPreferredTransportMode(string transportMode)
        +void calculateCarbonFootprint(): void
        +void commute(): void
        +void evaluateTransportOptions(): void
    }
